# A simple UNIX makefile
# TODO: update so that linking is done separately
# TODO: Also dependency issue, maybe fix with separate dependency script?
build:
	go build config.go main.go proxy.go server.go
clean:
	go clean
